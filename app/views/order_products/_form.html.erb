<section>
  <%= form_for @order_product do |f| %>
  <!-- <%= f.hidden_field :order_id, :value => @order_product.product_id %> -->
  <%= f.hidden_field :product_id, :value => @order_product.product_id %>

  <%= f.label :quantity %>
  <%= f.text_area :quantity %>
  <%= f.submit "Update", class: "button" %>



  <% end %>


</section>
